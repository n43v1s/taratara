# Work Item: Dry-Run Flow — 27-06-2025

## Status: Completed

## Yang Dilakukan

Membangun dry-run flow: build payload dari config + mapping, preview di UI, simpan log dan run record.

## File Yang Dibuat

| File | Keterangan |
|---|---|
| `app/services/scheduler_runner.py` | `build_payload()`, `run_dryrun()`, `list_runs()`, `get_run()`, `read_log()` |
| `app/routers/dryrun.py` | Route `GET /dryrun/` dan `POST /dryrun/run` |
| `app/templates/dryrun.html` | Halaman dry-run: status validator, tombol, ringkasan, payload preview, log |

## Fitur

- Validator `check_ready()` dijalankan sebelum dry-run — jika ada blocking issue, tombol tidak muncul dan pesan error ditampilkan
- Payload dibangun dari `mappings` (internal field → entry ID) + `config` (nilai per internal field)
- Checkbox fields (bookingDates, gender, dll.) ditangani sebagai multiple values
- Run record disimpan ke `data/runs/{run_id}.json`
- Log disimpan ke `data/logs/{run_id}.log`
- Hasil ditampilkan: run ID, waktu, endpoint, payload preview per entry ID, log output

## Verifikasi (run-20260627-052748-c4685b)

- 10 field terpetakan ke entry ID yang benar ✓
- Nilai dari config muncul dengan benar (nama, TTL, usia, dll.) ✓
- Tanggal booking muncul sebagai multiple values (2 Juni, 3 Juni, 4 Juni, 5 Juni) ✓
- Form tidak dikirim ✓
- Run record dan log tersimpan di `data/` ✓
- Link "Lanjut ke Prepare Submit" muncul setelah dry-run berhasil ✓
