# To Do — 27-06-2026

Urutan pengerjaan logis untuk membangun Tara Local Web Control Panel dari nol.

---

## 1. Setup Lingkungan

- [x] Buat `.gitignore` (exclude `.venv/`, `data/logs/`, `data/runs/`, `data/scheduler.lock`, `__pycache__/`, `.env`)
- [x] Buat `.venv` menggunakan Python 3.14.6 (`py -3.14 -m venv .venv`)
- [x] Buat `requirements.txt` dengan dependensi yang dipinned:
  - `fastapi==0.138.1`
  - `jinja2==3.1.6`
  - `uvicorn`
  - `httpx`
  - `beautifulsoup4`
  - `rapidfuzz`
  - `python-multipart` (untuk form submission di FastAPI)
- [x] Install dependensi ke `.venv`
- [x] Vendor HTMX 2.0.10 ke `app/static/vendor/htmx.min.js`
- [x] Buat `.env.example`

## 2. Skeleton FastAPI

- [x] Buat `app/main.py` — FastAPI entrypoint dengan static files dan Jinja2 templates
- [x] Buat `app/routers/` — direktori kosong dengan `__init__.py`
- [x] Buat `app/services/` — direktori kosong dengan `__init__.py`
- [x] Buat `app/templates/` — base layout template (`base.html`)
- [x] Buat `app/static/css/` — stylesheet dasar
- [x] Tambah route health-check (`GET /health`) dan dashboard placeholder (`GET /`)
- [x] Verifikasi: server bisa jalan, HTMX termuat, template render benar

## 3. Data Layer & JSON Files

- [x] Buat direktori `data/`, `data/runs/`, `data/logs/`
- [x] Buat `data/tara-caraka-form.config.json` — struktur awal dengan semua internal fields kosong
- [x] Buat `data/form-profile.json` — struktur awal kosong
- [x] Buat `data/field-aliases.json` — alias awal untuk semua 11 internal fields (dari contoh di README)
- [x] Buat `app/services/config_handler.py` — read/write JSON config dengan validasi

## 4. Google Form Parser Service

- [x] Buat `app/services/form_parser.py`:
  - Fetch Google Form HTML via `httpx`
  - Extract form ID, response URL (`action` attribute)
  - Extract semua field: label, `entry.*` ID, type, options
  - Extract booking date options (checkbox/radio field)
  - Extract `fbzx` fallback value
- [x] Buat router `app/routers/form.py` — route `POST /form/parse`
- [x] Buat template form input URL dan hasil parsing

## 5. Field Matching Service

- [x] Buat `app/services/field_matcher.py`:
  - Layer 1: exact match + alias match dari `field-aliases.json`
  - Layer 2: fuzzy match dengan `rapidfuzz`
  - Layer 3: tandai unmapped untuk review manual
  - Normalisasi label (trim, lowercase, hapus spasi berlebih)
  - Output: mapping dengan confidence level (high/medium/low/unmapped)
- [x] Integrasi ke form parser flow — jalankan matcher setelah parse

## 6. Field Mapping Review UI

- [x] Buat router `app/routers/mapping.py` — route review & konfirmasi mapping
- [x] Buat template mapping review:
  - Tabel: label asli | internal field | confidence | status
  - Aksi per baris: accept / pilih manual
  - Tombol "Konfirmasi Semua" hanya aktif jika semua required fields terpetakan
- [x] Simpan mapping yang dikonfirmasi ke `data/form-profile.json`

## 7. Config Editor

- [x] Buat router `app/routers/config.py` — route `GET/POST /config`
- [x] Buat template config editor:
  - Form input per internal field — dropdown/radio dari opsi form nyata jika tersedia
  - Validasi: required fields tidak boleh kosong
  - Save ke `data/tara-caraka-form.config.json`
  - Simpan `configUpdatedAt` saat nilai payload field berubah

## 8. Booking Date Selector

- [x] Integrasi ke config editor
- [x] Tampilkan opsi tanggal dari `form-profile.json` sebagai checkbox
- [x] Validasi: minimal satu tanggal harus dipilih sebelum dry-run/submit

## 9. Validation Layer

- [x] Buat `app/services/validator.py`:
  - Cek semua required fields ada di config
  - Cek semua required field mappings sudah confirmed
  - Cek booking dates dipilih dan valid (ada di form profile)
  - Cek tidak ada active scheduler lock sebelum submit baru
  - Cek dry-run sudah dijalankan setelah `configUpdatedAt` (hanya untuk submit)
- [x] Gunakan validator di semua route dry-run, prepare submit, confirm submit

## 10. Dry-Run Flow

- [x] Buat router `app/routers/dryrun.py` — route `POST /dryrun/run`, `GET /dryrun/result/{run_id}`
- [x] Buat `app/services/scheduler_runner.py`:
  - Launch `tara-caraka-form.ps1 -TestDryRun` sebagai subprocess
  - Capture stdout/stderr
  - Tulis log ke `data/logs/`
  - Buat run record di `data/runs/`
- [x] Buat template dry-run report: run ID, payload preview, jadwal attempt, status, tombol "↺ Dry Run Ulang"

## 11. PowerShell Scheduler Script

- [x] Buat `tara-caraka-form.ps1`:
  - Baca `data/tara-caraka-form.config.json` dan `data/form-profile.json`
  - Mode `-TestDryRun`: build payload, cetak preview, tidak submit
  - Mode `-Mode Submit`: jadwalkan async submit attempts (12:59:58, 12:59:59, 13:00:00)
  - Tulis log ke `data/logs/` dan run record ke `data/runs/` tanpa BOM (`[System.IO.File]::WriteAllText`)
  - Tulis lock file setelah start (bukan FastAPI yang nulis)
  - Cegah laptop sleep selama window submit
  - Parameter `-AttemptTimesOverride` untuk developer test submit
  - Deteksi connection-closed dari Google Forms sebagai sukses
  - `-NoExit` + `try/catch/finally` agar terminal tidak langsung tutup

## 12. Submit Flow (Prepare → Confirm)

- [x] Route `POST /submit/prepare` — validasi, buat pending run, tampilkan summary
- [x] Route `POST /submit/confirm` — launch PowerShell submit, return status
- [x] Route `POST /submit/cancel` — cek safety, stop process, hapus lock
- [x] Route `POST /submit/test` — developer test submit dengan delay yang bisa dikonfigurasi
- [x] Template prepare submit: summary lengkap + tombol Confirm Submit + tombol edit config
- [x] Lock file ditulis oleh PowerShell, bukan FastAPI (fix race condition)

## 13. Scheduler Status & Monitoring

- [x] Route `GET /status` — baca lock, process state, log tail
- [x] Template status: active/inactive, run ID, PID, next attempt, last N log lines
- [x] HTMX polling untuk auto-refresh log aktif via `GET /logs/{run_id}/fragment`

## 14. Logs & Run History

- [x] Route `GET /logs` — list semua run records, dikelompokkan per sesi
- [x] Route `GET /logs/{run_id}/fragment` — HTMX polling fragment untuk live log
- [x] Template logs: run history per session (collapsible `<details>`) + log file preview

## 15. Reset Page

- [x] Route `GET /reset`, `POST /reset/form`, `POST /reset/runs`, `POST /reset/lock`
- [x] Template: tombol reset form/mapping, reset run history, hapus scheduler lock

## 16. Dashboard Enhancements

- [x] Flow diagram 5 langkah dengan state done/ready/blocked/active
- [x] Banner "Sesi selesai" + tombol "Mulai Sesi Baru" setelah submit berhasil
- [x] Developer card untuk test submit (delay configurable, log live di bawah card)
- [x] Session concept: `sessionId` + `sessionLabel` di form-profile.json, semua run record punya `sessionId`

## 17. Help Page

- [ ] Route `GET /help`
- [ ] Template: workflow steps, safety rules, penjelasan tiap aksi

## 18. Test Suite

- [ ] `tests/test_form_parser.py` — unit test parser dengan HTML fixture
- [ ] `tests/test_field_matcher.py` — unit test alias match, fuzzy match, unmapped
- [ ] `tests/test_config_handler.py` — unit test read/write/validate config
- [ ] `tests/test_validator.py` — unit test blocking conditions

## 19. Dev Helper

- [x] Buat `scripts/dev.ps1` — satu script untuk aktifkan venv + start uvicorn

---

**Target pertama yang bisa dijalankan:** setelah langkah 1–2 selesai, server sudah bisa jalan di `http://127.0.0.1:8000`. ✅

**Target MVP:** setelah langkah 1–12 selesai, full flow parse → review → config → dry-run → submit sudah bisa dijalankan. ✅

**Sisa:** Help page (#17) dan test suite (#18).
